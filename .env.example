# Database — PostgreSQL connection string (Neon, Supabase, local Docker, etc.)
DATABASE_URL="postgresql://user:password@host:5432/bookingplatform?sslmode=require"

# NextAuth
NEXTAUTH_URL="https://your-app.vercel.app"
NEXTAUTH_SECRET=""  # Generate with: openssl rand -base64 32

# ─── Agent Framework ──────────────────────────────────────────
# LLM provider: "gemini" (default) or unset to force the stub provider.
AGENT_LLM_PROVIDER="gemini"

# Google Gemini API key — free tier available at https://aistudio.google.com/.
# When unset the agents fall back to the stub provider (no-op decisions).
AGENT_GEMINI_API_KEY=""
AGENT_GEMINI_MODEL="gemini-1.5-flash"

# Weather provider for the Triage Agent. Get a key at
# https://openweathermap.org/api (free One Call API 3.0).
AGENT_OWM_API_KEY=""

# Shared secret used by the agent run endpoint when invoked by cron.
# Send as `Authorization: Bearer $AGENT_SECRET`.
AGENT_SECRET=""

# Base URL for the run-agent CLI script. Defaults to http://localhost:3000.
AGENT_BASE_URL=""

# Detection Agent: max chat messages to fetch per run.
AGENT_BATCH_SIZE="200"
