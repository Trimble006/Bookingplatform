# Production service dependencies.
# Loose lower bounds so pip can select wheels available for the host Python
# (local dev may use Python 3.14; Docker uses 3.12 where older pinned versions
# are fine). Strict pins belong in a lockfile, not here.
fastapi>=0.116.0
uvicorn[standard]>=0.34.0
pandas>=2.2.0
scikit-learn>=1.5.0
psycopg[binary]>=3.1.0
joblib>=1.4.0
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.0.0
